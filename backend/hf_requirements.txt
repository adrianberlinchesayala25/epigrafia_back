# EpigrafIA - Render Requirements
# Python 3.11 compatible

# Web framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9

# ML - Use CPU version if possible to save size/memory
# TensorFlow is heavy. We try to keep it minimal.
tensorflow>=2.15.0

# Audio processing
librosa>=0.10.1
soundfile>=0.12.1
# pydub is optional but good for webm
pydub>=0.25.1
numpy>=1.26.0,<2.0.0
